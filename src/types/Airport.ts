export type Airport = {
  city: string,
  city_code: string,
  country_code: string,
  iata_code: string,
  icao_code: string,
  lat: number,
  lng: number,
  name: string,
  popularity: number,
  slug: string,
  timezone: string,
}